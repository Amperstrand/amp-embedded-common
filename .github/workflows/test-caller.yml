# Test workflow that calls the reusable workflow
name: Test Caller

on:
  push:
    branches: [main]

jobs:
  call_test:
    uses: Amperstrand/amp-embedded-common/.github/workflows/test-reusable.yml@main
    with:
      test_input: "world"